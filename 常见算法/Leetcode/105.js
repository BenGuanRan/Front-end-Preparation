var buildTree = function (preorder, inorder) {

    const createTree = (val, left, right) => {
        // 叶子节点
        if (left === null && right === null) {
            return {
                val,
                left: null,
                right: null
            }
        }
        return createTree

    }
};